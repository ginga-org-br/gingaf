name: ncldoc
description: "A pure Dart implementation of the Nested Context Language (NCL) document model."
version: 0.0.1

environment:
  sdk: ^3.11.0

dependencies:
  logging: ^1.3.0
  lua_dardo_plus: ^0.3.0
  xml: ^7.0.1

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
