name: ccws
description: "Ginga-CC-WebService"
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  logging: ^1.3.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  http: ^1.1.0
  test: ^1.29.0
  lints: ^6.0.0
