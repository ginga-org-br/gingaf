name: nclui
description: "Flutter widgets and UI components for rendering NCL media and documents."
version: 0.0.1

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  ncldoc:
  logging: ^1.3.0
  video_player: ^2.14.0
  webview_all: ^1.0.3
  gingacc: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  video_player_platform_interface: ^6.9.0
  plugin_platform_interface: ^2.1.8
