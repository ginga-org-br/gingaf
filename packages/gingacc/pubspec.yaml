name: gingacc
description: "Ginga common configuration and core package"
version: 0.0.1

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  logging: ^1.3.0
  http: ^1.1.0
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  web: ^1.0.0
  json5: ^0.8.2

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
